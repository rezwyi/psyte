= link_to t(".new_#{controller_name.singularize}"),
          url_for(action: :new, controller: controller_name),
          class: 'btn btn-success'

table.table
  thead
    tr
      th #
      th = t('.title')
      th = t('.created_at')
      th
  tbody
    - @resources.each do |r|
      tr
        td = r.id
        td = link_to r.title, post_path(r.id), target: '_blank'
        td = Russian::strftime r.created_at, '%d %B %Y'
        td.actions
          = link_to t('.edit'), edit_admin_post_path(r.id), class: 'btn'
          | &nbsp;
          = link_to t('.delete'), admin_post_path(r.id), method: :delete,
                    confirm: I18n.t('messages.really?'), class: 'btn btn-danger'
