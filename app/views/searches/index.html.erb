<% content_for :title, title(t('common.search')) %>

<div class="search-posts">
  <%= t('.search_posts') + ' ' %>"<b><%= params[:q] %></b>" (<%= t('common.count') + ' ' + @posts.count.to_s %>)
</div>

<%= render @posts %>
